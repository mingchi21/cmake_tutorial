{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmakebuild",
            "type": "shell",
            "command": "cmake --build build --config Debug --target all -- -j 6"
        }
    ]
}